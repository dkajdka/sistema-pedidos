<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>editar_usuario</title>
    <link rel="stylesheet" href="/css/style.css" type="text/css">
</head>
<body>
    <%- include('../../public/menuAdmin.html') %>
    
    <h1>Editar Usuário</h1>

    <form action="/usuario/salvar/<%= usuario[0].id %>" method="post">
        <label>Nome</label>
        <br>
        <input type="text" name="nome" id="nome" value="<%= usuario[0].nome%>">
        <br>
        <label>E-mail</label>
        <br>
        <input type="text" name="email" id="email" value="<%= usuario[0].email%>">
        <br>
        <label>Tipo Usuário</label>
        <br>
        <select name="id_tipo_usuario">
          <% for(let i = 0; i < tipos.length; i++) {
            if (usuario[0].id_tipo_usuario == tipos[i].id){
            %>
                <option value="<%= tipos[i].id %>" selected><%= tipos[i].descricao %></option>
            <%}
                else {
            %>
                <option value="<%= tipos[i].id %>"><%= tipos[i].descricao %></option>               
            <% }} %>  
        </select>
        <input type="submit" value="Salvar">
        <br><br> 
    </form>
</body>
</html>