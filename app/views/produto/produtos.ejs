<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>editar_produto</title>
    <link rel="stylesheet" href="/css/style.css" type="text/css">
</head>
<body>
    <%- include('../../public/menuAdmin.html') %>

    <h1>Catálogo de Produtos</h1>

    <table>
        <tr>
            <th>Descrição</th> 
            <th>Preço</th>                 
            <th></th>
        </tr>
        <% for (let i = 0; i < produto.length; i++) { %>
            <tr>
                <td><%= produto[i].descricao %></td>    
                <td><%= produto[i].preco %></td>                       
                <td>
                    <a href="/produto/editar/<%=produto[i].id %>">Editar</a>
                    <a href="/produto/excluir/<%=produto[i].id %>">Excluir</a>
                </td>
            </tr>
        <% } %>       
    </table>
</body>
</html>