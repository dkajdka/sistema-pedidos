<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>lista_produto</title>
    <link rel="stylesheet" href="/css/style.css" type="text/css">
</head>
<body>
    <%- include('../../public/menu.html') %>

    <h1>Catálogo de Produtos</h1>

    <table>
        <tr>
            <th>Descrição</th> 
            <th>Preço</th>                 
            <th></th>
        </tr>
        <% for (let i = 0; i < produto.length; i++) { %>
            <tr>
                <td><%= produto[i].descricao %></td>    
                <td><%= produto[i].preco %></td>                       
                <td>
                    <a href="/produto/adicionar_produto/<%=produto[i].id %>">Adicionar</a>
                </td>
            </tr>
        <% } %>       
    </table>
</body>
</html>